# Raft Replicated Database

A python implementation of Raft algorithm that allows to create a reliable replicated key-value storage with http api

## How to Run

```bash
docker compose up --build
```
